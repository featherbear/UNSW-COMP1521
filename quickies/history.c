#include <stdio.h>
#include <stdlib.h>

#define MAXHIST 20
#define MAXSTR 200

int main() {
printf("hh" "aa");
   char temp[MAXSTR];
 //  int cmdNo = 0;
/*
   int i;

   char **a;
   for (i = 0; i < MAXHIST; i++) a[i] = calloc(MAXSTR, sizeof(char));
   FILE *f = fopen("mymysh_history", "r");
   i = 0;

    printf("sss");

   while (fgets(temp, sizeof(temp), f)) {
      printf("%s\n", temp);
      sscanf(temp, "%[^;]""\n", a[i]);
//      cmdNo = commandHistory.commands[i].seqNumber;
      i++;
   }


   fclose(f);
*/
   return 0;

}
